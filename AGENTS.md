# Repository Notes

- After each completed code change, commit and push automatically unless the user says not to.
- Leave the untracked `imbibition/` directory alone unless the user explicitly asks to modify or commit it.
